from main import app
# Number of worker processes to handle requests
# Adjust this based on your application's needs and server resources
workers = 4

# Address and port to bind the server to
# By default, it will bind to localhost on port 8000
bind = "127.0.0.1:8000"
